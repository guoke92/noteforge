- 面板组件通常导出为命名函数组件（如 `export function Sidebar`），并采用 PascalCase 命名以匹配 React 规范。
- 使用 `lucide-react` 图标库配合 `Tooltip` 组件构建导航按钮，通过 `aria-pressed` 属性标识当前激活状态。
- 在功能特性模块（features）中，复杂视图（如图谱、AI 面板）被封装为独立组件，并通过懒加载或直接导入的方式集成到主面板中。