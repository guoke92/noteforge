- 核心服务采用工厂函数模式（如 `createDocumentService`）并在 `runtime.ts` 中统一组装为单例。
- UI 组件遵循原子化设计，基础交互组件存放于 `components/ui`，业务面板存放于 `components/{sidebar,right,editor}`。
- 异步启动步骤通过 `useStartupStore` 进行状态追踪，确保 Splash 屏幕在关键资源（主题、工作区、会话）加载完成后平滑过渡。