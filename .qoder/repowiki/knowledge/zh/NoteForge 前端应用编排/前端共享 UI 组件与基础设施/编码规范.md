- UI 组件统一使用 forwardRef 包装并显式设置 displayName，支持 ref 转发与调试。
- IPC 调用采用 call 高阶函数，根据 isTauri() 结果动态分发至真实 invoke 或 stub 模拟实现。
- Zustand Store 定义中集成异步初始化方法（如 init, restore），并在组件外通过 getState/setState 进行跨模块交互。
- 工具函数优先导出纯函数（如 cn, basename），避免在 lib 中直接依赖 React 上下文或全局状态。