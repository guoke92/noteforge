- **服务组装**：`runtime.ts` 作为入口，实例化 `Vault`、`Document`、`Workbench` 等服务并注入共享的 `EventBus`。
- **跨域同步**：利用 `editor-store-bridge` 和 `editor-sync` 桥接底层文档模型与 UI 状态，确保多视图一致性。
- **契约约束**：通过 `invariants.ts` 定义架构不变量（如单一内容源、只读知识库），强制各子模块遵守边界。
- **事件驱动**：所有跨模块通信（如文件变更触发索引更新）均通过 `events.ts` 定义的强类型事件流进行解耦。