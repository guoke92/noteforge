- 采用 Revision 脏检测机制：通过 `buildStatRevision` 或 `buildDiskRevision` 生成文件指纹，对比内存快照以识别磁盘变更，避免全量内容读取。
- 路径规范化处理：在打开知识库或处理文件路径时，统一执行 `replace(/\/+$/, "")` 去除尾部斜杠，确保路径标识的一致性。
- 异步性能埋点：在关键 I/O 操作（如 `open`）中使用 `perfAsync` 和 `perfLog` 包裹，用于监控启动及运行时性能指标。