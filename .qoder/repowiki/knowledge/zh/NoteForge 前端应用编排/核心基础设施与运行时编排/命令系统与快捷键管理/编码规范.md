- 命令定义采用 `cmd()` 辅助函数包装，统一处理 `CommandDefinition` 类型约束与可选快捷键配置。
- 快捷键绑定支持 `when` 上下文子句（如 `!inputContext`, `markdown`），在执行前通过 `matchesWhen` 进行细粒度权限校验。
- 所有命令执行均注入统一的 `CommandContext`，确保命令逻辑与当前编辑器状态、焦点环境解耦。
- 核心命令 ID 统一定义在 `CORE_COMMANDS` 常量对象中，避免魔法字符串散落在注册逻辑里。