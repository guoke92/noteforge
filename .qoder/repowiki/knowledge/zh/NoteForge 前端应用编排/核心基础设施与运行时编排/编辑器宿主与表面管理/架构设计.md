- 采用服务化架构，通过 `createEditorHostService` 工厂函数创建 `EditorHostService` 实例。
- 定义 `LiveSurfaceHandle` 接口作为运行时绑定契约，解耦具体编辑器实现（如 Milkdown/Monaco）与核心逻辑。
- 内部维护 `Map` 存储表面句柄，通过 `surfaceRegistrationKey` 实现文档ID与模式（write/source/read）的复合索引。
- 遵循 ADR-005 规范，在模式切换或失焦时强制调用 `flush` 将局部变更转化为 `ContentPatch` 并向上游文档服务提交。