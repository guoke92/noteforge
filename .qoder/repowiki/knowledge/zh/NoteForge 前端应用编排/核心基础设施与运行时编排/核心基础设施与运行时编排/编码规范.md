- 使用 `createDebouncedFlush` 泛型工厂统一处理异步状态的防抖调度与强制刷新，避免在各业务模块重复实现定时器逻辑。
- 在 `runtime.ts` 中采用动态导入（`void import(...)`）处理副作用逻辑（如 Local History 快照），以优化核心路径的启动性能。
- 所有跨模块的状态变更必须通过 `EventBus` 派发标准化事件，禁止组件或服务直接调用其他领域的写操作方法。