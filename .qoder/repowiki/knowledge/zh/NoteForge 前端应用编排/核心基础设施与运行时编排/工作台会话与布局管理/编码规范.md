- 会话持久化采用防抖策略，根据变更类型（内容 vs 布局）应用不同的延迟阈值（`SESSION_PERSIST_CONTENT_DEBOUNCE_MS` / `LAYOUT_DEBOUNCE_MS`）。
- 恢复流程采用“活跃标签优先”策略，并行处理非活跃标签的延迟挂载（`mountDeferredTabShell`）以优化启动性能。
- 所有涉及 I/O 或复杂逻辑的公共方法均包裹 `perfAsync` 并配合 `perfLog` 记录执行耗时与关键上下文。