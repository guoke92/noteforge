- 采用轻量级服务层设计，通过 `service.ts` 封装核心逻辑，`types.ts` 定义数据契约。
- 依赖注入采用动态导入（Dynamic Import）策略处理 `@/ipc` 模块，以优化启动性能并解耦进程间通信。
- 内部维护 `autoTimers` Map 实现多文件维度的定时自动快照（5分钟间隔），并提供全量清理接口以应对应用退出场景。
- 所有异步操作均包含统一的错误捕获与静默失败处理，确保历史记录功能的非侵入性。