- 所有文档状态变更（打开、保存、应用补丁）均通过 `setRecord` 统一更新内存 Map 并触发 `eventBus` 事件，确保 UI 与核心逻辑解耦。
- 大文件处理遵循“预览优先”原则，huge 级文件在初始打开时 `contentLoaded` 标记为 false，仅在用户显式编辑或保存前才执行全量磁盘读取。
- 冲突解决采用交互式决策流，在保存或恢复会话时若检测到磁盘内容与 baseline 不一致，通过 `promptSaveConflict` 等对话框让用户选择保留本地、重载磁盘或另存为。