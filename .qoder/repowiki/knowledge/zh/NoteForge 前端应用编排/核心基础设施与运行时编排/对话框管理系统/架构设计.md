- 采用分层架构：`types.ts` 定义 `DialogRequest` 联合类型与 `DialogService` 接口；`dialog-store.ts` 使用 Zustand 维护 active/queue 状态；`dialog-service.impl.ts` 实现队列逻辑（如关闭类对话框的优先级处理）；`dialog-api.ts` 提供基于 `getCore()` 的全局便捷调用入口。
- 引入 `draft-prompt.ts` 作为特殊交互层，通过 Promise 封装异步用户决策（如草稿恢复、保存冲突），实现 UI 触发与业务逻辑解耦。