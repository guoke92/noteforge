- 全局对话框通过 Zustand Store 中的布尔状态（如 `globalSearchOpen`）控制显隐，并在 `App.tsx` 根节点统一挂载，确保层级最高且不受局部布局影响。
- 系统级确认对话框（如保存冲突、删除确认）采用集中式 `DialogHost` 模式，根据 `dialog-store` 中的 `active.kind` 字段动态渲染对应的子组件。
- 复杂表单类对话框（如新建记忆、导入向导）倾向于独立封装为专用组件，内部自行管理表单状态，仅在提交时调用 IPC 或 Core API。