- 全局对话框通过独立的 `DialogHost` 组件根据 Store 状态统一渲染，而非在各处分散调用。
- 启动屏 (`AppSplashScreen`) 与主工作区 (`MainWorkbench`) 基于 `bootstrapComplete` 状态互斥显示，确保视觉连贯性。
- 顶部导航栏 (`TopBar`) 集成下拉菜单 (`Dropdown`) 与快捷键提示，提供文件、视图、工具等全局操作入口。