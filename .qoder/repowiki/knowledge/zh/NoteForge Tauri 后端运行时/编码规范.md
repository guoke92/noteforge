- 所有对外暴露的业务功能均通过 `src/commands/` 下的函数实现，并统一注册到 Tauri IPC 处理器中。
- 采用仓储模式（Repository Pattern），在 `src/repositories/` 中封装对 SQLite 数据库及文件系统的底层访问逻辑。
- 使用 `thiserror` 和 `anyhow` 构建统一的错误处理体系，定义在 `src/error.rs` 中供各模块复用。