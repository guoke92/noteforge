- 所有 Tauri 命令函数均通过 `State<'_, ConfigManager>` 或 `State<'_, Database>` 注入依赖，保持无状态处理逻辑。
- 索引操作（`index_document` / `remove_document`）严格包裹在 SQLite 事务（BEGIN/COMMIT/ROLLBACK）中以确保数据一致性。
- AI 服务方法统一提供可选的 `model` 参数，若未指定则回退到硬编码的默认模型标识符。
- 实体提取（标签与链接）采用正则表达式匹配，同时支持 Markdown 语法与 YAML Frontmatter 格式。