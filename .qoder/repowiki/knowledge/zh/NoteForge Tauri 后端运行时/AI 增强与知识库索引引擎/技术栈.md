- **LLM 集成**：通过 `reqwest` 对接 Ollama (`/api/generate`, `/api/tags`)，默认模型为 `llama3`。
- **向量嵌入**：使用 `fastembed` 库生成文本嵌入，存储在 SQLite 的 JSON 字段中。
- **全文检索**：基于 SQLite `fts5` 扩展实现 Unicode 感知的全文搜索。
- **差异计算**：使用 `diff` crate 生成 AI 润色内容的行级差异对比。