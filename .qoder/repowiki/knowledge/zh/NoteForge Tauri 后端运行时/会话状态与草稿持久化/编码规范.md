- 所有涉及文件写入的操作均遵循“先写入 .tmp 临时文件，再 fs::rename 原子替换目标文件”的模式，以防止数据损坏。
- Tauri 命令函数统一使用 `State<'_, StoreType>` 注入依赖，并返回 `Result<T, NoteforgeError>` 以统一错误处理。
- 存储结构体（Store）的构造函数 `new` 接收 `&AppHandle` 以解析应用数据目录（app_data_dir），确保路径隔离。