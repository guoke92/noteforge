- 原子写模式：所有涉及文件更新的操作（会话保存、草稿保存）均采用先写入临时文件（.tmp）再重命名（rename）的方式，防止进程崩溃导致数据损坏。
- 错误处理统一化：模块内所有公共函数返回 `Result<T, NoteforgeError>`，并通过 `map_err` 将底层 IO、JSON 等错误转换为统一的领域错误类型。
- 路径哈希映射：使用 `DefaultHasher` 将用户友好的 Vault 路径转换为固定长度的十六进制字符串作为底层存储文件名，避免文件系统兼容性问题。