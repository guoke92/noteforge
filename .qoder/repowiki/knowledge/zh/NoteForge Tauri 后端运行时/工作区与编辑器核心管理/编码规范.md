- 所有 Tauri 命令函数均返回 `Result<T, NoteforgeError>` 以统一错误处理。
- Repository 构造函数统一接收 `&'a Connection` 引用，实现无状态的数据访问逻辑。
- 模型结构体统一使用 `#[serde(rename_all = "camelCase")]` 以适配前端命名规范。
- 文件路径操作前必须调用 `ensure_real_file_path` 拦截虚拟路径或非法协议头。