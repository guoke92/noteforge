- 所有仓库类（Repo）均采用生命周期绑定的构造函数 `new(&'a Connection)`，确保数据库连接在事务或调用栈中的借用安全性。
- 命令函数统一返回 `Result<T, NoteforgeError>`，并通过 `map_err(NoteforgeError::Io)` 等模式将底层系统错误转换为应用层自定义错误。
- 数据模型结构体统一使用 `#[serde(rename_all = "camelCase")]` 注解，确保 Rust 蛇形命名与前端 JavaScript 驼峰命名的自动兼容。