- 所有对外暴露的 Tauri 命令函数均使用 `#[tauri::command]` 宏标记，并返回 `Result<T, NoteforgeError>` 以统一错误响应格式。
- 数据传输对象（DTO）在 `models/` 子模块中定义，严格使用 `#[serde(rename_all = "camelCase")]` 以确保与前端 JavaScript 命名规范兼容。
- 核心服务结构体（如 `ConfigManager`, `EncryptionService`）的方法倾向于接收不可变引用或内部使用 `Mutex` 处理可变状态，以适配 Tauri 的多线程运行时环境。