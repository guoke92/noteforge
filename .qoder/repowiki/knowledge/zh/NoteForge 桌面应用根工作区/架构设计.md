- 通过 `vite.config.ts` 配置本地开发服务器（端口 1420）并与 Tauri CLI 深度集成，实现前后端联调的自动化引导。
- 利用 `tsconfig.json` 的路径别名（`@/*`）与 `exclude` 规则，在编译层面明确隔离前端源码与 `src-tauri` 后端逻辑，确保模块边界清晰。
- 依托 `package.json` 中的统一脚本（如 `tauri:dev`），将 Vite 的热更新能力与 Tauri 的原生窗口管理绑定，形成单一入口的开发工作流。